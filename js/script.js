/**
 * @file
 * Global JavaScript for the dreamSteps theme.
 */

(function ($, Drupal) {
  'use strict';

  /**
   * Global behaviors for dreamSteps theme.
   */
  Drupal.behaviors.dreamstepsGlobal = {
    attach: function (context, settings) {
      // Add global theme behaviors here.
      console.log('dreamSteps theme loaded');
    }
  };

})(jQuery, Drupal);
